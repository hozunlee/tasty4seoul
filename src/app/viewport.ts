import { Viewport } from 'next';

export const viewport: Viewport = {
  themeColor: '#FF5A36',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  viewportFit: 'cover',
}
